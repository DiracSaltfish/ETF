# 万得股票实时申购赎回接口研究

## 一、结论摘要

万得股票客户端中的 ETF“实时申购赎回信息”来自内部 TBAPI2 数据订阅。

当前已经验证：

- 可以从外部临时附加到已经登录的万得进程。
- 可以在万得进程内加载自定义只读探针。
- 可以复用进程内已经建立的 TBAPI2 客户端实例，不需要读取用户名、密码或会话令牌。
- 目标数据不是普通的一次性查询，而是使用 `CreateSubscription → ModifySubscription → 回调` 获取的实时订阅数据。
- 本次使用 `CJAVAQuery` 的一次性查询被 TBAPI2 在提交阶段拒绝，返回错误码 `-12314`。下一步应改用订阅接口。

整体判断：**这条路径在技术上可行。进程附加、动态加载和登录态复用已经验证成功，剩余核心工作是还原订阅回调并解析返回的数据帧。**

## 二、目标数据及字段

万得客户端页面使用的 SQL 为：

```sql
SELECT etfbuynumber,
       etfbuyamount,
       etfbuymoney,
       etfsellnumber,
       etfsellamount,
       etfsellmoney
FROM ETFComprehensive.WholeETFData
WHERE windcode = '159518.SZ'
LATENCY(500 MS)
```

字段含义如下：

| 字段 | 含义 |
|---|---|
| `etfbuynumber` | 实时申购笔数 |
| `etfbuyamount` | 实时申购份额 |
| `etfbuymoney` | 实时申购金额 |
| `etfsellnumber` | 实时赎回笔数 |
| `etfsellamount` | 实时赎回份额 |
| `etfsellmoney` | 实时赎回金额 |

截图中的示例数据为：

| 指标 | 申购 | 赎回 |
|---|---:|---:|
| 笔数 | 3 | 2 |
| 金额 | 0 | 0 |
| 份额 | 300 万 | 200 万 |

## 三、客户端内部接口

目标动态库位于：

```text
/Applications/WindPersonFree.app/Contents/Frameworks/libWind.Cosmos.TBAPI2.dylib
```

已经确认的相关导出函数包括：

```text
CJAVAQuery
CJAVACreateSubscription
CJAVAModifySubscription
CJAVARegisterQueryCallBack
```

其中 `CJAVAQuery` 对应的内部 C++ 方法签名为：

```cpp
JavaAPIModule::Query(
    long long requestId,
    const char* sql,
    unsigned int timeout,
    bool stateful
)
```

`CJAVAQuery` 最终会调用：

```cpp
cosmos::kernel::ICosmosClient::GetInst()
```

这说明它使用的是当前进程中的全局 TBAPI2 客户端实例。因此，在万得进程内部调用时，理论上可以直接复用当前登录连接。

## 四、已经完成的实际验证

### 4.1 编译只读探针

已经编译一个最小探针，只允许执行指定的 ETF 申购赎回查询。

探针不执行以下操作：

- 不读取用户名或密码。
- 不复制登录令牌。
- 不修改万得客户端安装文件。
- 不执行交易指令。
- 不接受外部传入的任意 SQL。

相关文件：

```text
/Users/ellis/Desktop/ETF交割/wind_tbapi_probe/probe.c
/Users/ellis/Desktop/ETF交割/wind_tbapi_probe/README.md
```

### 4.2 附加到万得进程

使用调试器临时附加到了已经登录的万得进程。附加成功后，客户端只发生短暂停顿，随后立即分离。

这一步证明 macOS 当前环境允许经过授权后附加万得进程。

### 4.3 处理应用沙盒限制

最初从桌面目录加载探针时，万得应用沙盒拒绝访问：

```text
file system sandbox blocked open()
```

随后将探针临时放入万得自己的沙盒目录：

```text
/Users/ellis/Library/Containers/com.windin.mac.free/Data/tmp/wind_tbapi_probe/
```

再次加载成功，`dlopen` 返回了有效句柄：

```text
0x65c5f4d0
```

这证明外部探针可以在不修改万得安装包的情况下进入万得进程。

### 4.4 一次性查询结果

探针调用 `CJAVAQuery` 后得到：

```json
{
  "status": "submit_error",
  "code": -12314
}
```

结果文件位于：

```text
/Users/ellis/Library/Containers/com.windin.mac.free/Data/tmp/wind_tbapi_probe_result.json
```

这是 TBAPI2 在提交阶段返回的错误，不是进程附加、动态库加载或文件沙盒错误。

## 五、为什么一次性 Query 没有获得数据

进一步检查万得运行日志后确认，客户端页面没有使用普通的 `Query` 获取这组数据，而是使用订阅接口。

页面首先创建一个证券代码为空的订阅：

```text
CreateSubscription
subscription_id = 720897
windcode = ''
```

当用户切换到 `159518.SZ` 时，客户端再修改该订阅：

```text
ModifySubscription
subscription_id = 720897
windcode = '159518.SZ'
```

由于 SQL 中带有：

```sql
LATENCY(500 MS)
```

它本质上是一个每 500ms 推送更新的实时订阅，而不是一次性查询。

因此，本次验证得到的关键修正是：

> 目标数据确实可以通过 TBAPI2 获取，但正确入口应是订阅接口，而不是 `CJAVAQuery`。

## 六、剩余实现思路

下一步应复刻万得页面的真实订阅流程：

```text
已登录的万得进程
        ↓
注册 Subscription 回调
        ↓
CJAVACreateSubscription（windcode 为空）
        ↓
取得 subscription_id
        ↓
CJAVAModifySubscription（替换为 159518.SZ）
        ↓
每 500ms 收到 JavaTableFrame
        ↓
解析六个申购赎回字段
        ↓
通过 Unix Socket 或本地 HTTP 提供给外部程序
```

### 6.1 还原订阅回调

需要继续确定：

- 订阅回调的注册函数和准确签名。
- 回调参数中的订阅 ID、错误码和数据帧指针。
- 回调数据的生命周期。

回调返回后，TBAPI2 会释放部分缓冲区。因此，外部桥接程序必须在回调函数内部立即复制数据，不能只保存原始指针。

### 6.2 复刻创建和修改订阅

建议严格模仿客户端：

1. 创建 `windcode = ''` 的订阅。
2. 保存返回的 `subscription_id`。
3. 使用 `CJAVAModifySubscription` 将代码替换为 `159518.SZ`。
4. 在回调中接收更新。
5. 完成后取消订阅。

### 6.3 解析 JavaTableFrame

已经初步确认返回帧约为 `0x70` 字节，内部包含：

- 字段数量。
- 字段描述缓冲区。
- 数据缓冲区。
- 各缓冲区长度。
- 若干行数和帧状态信息。

剩余工作是将字段描述与数据值一一对应，最终输出类似：

```json
{
  "windcode": "159518.SZ",
  "timestamp": "2026-08-11T14:51:27+08:00",
  "etfbuynumber": 3,
  "etfbuyamount": 3000000,
  "etfbuymoney": 0,
  "etfsellnumber": 2,
  "etfsellamount": 2000000,
  "etfsellmoney": 0
}
```

具体原始数值的单位仍应根据返回字段元数据验证，不应仅根据页面上的“万”单位推断。

## 七、长期运行方案

如果订阅验证成功，可以在万得进程内建立一个非常小的只读桥接模块。

推荐使用 Unix Domain Socket：

```text
Wind 进程内 TBAPI2
        ↓
白名单订阅管理器
        ↓
Unix Domain Socket
        ↓
外部 Python / Go / Java 程序
```

也可以封装成本地 HTTP：

```http
GET /etf/realtime-redemption?windcode=159518.SZ
```

返回：

```json
{
  "windcode": "159518.SZ",
  "subscription_count": 3,
  "subscription_shares": 3000000,
  "subscription_money": 0,
  "redemption_count": 2,
  "redemption_shares": 2000000,
  "redemption_money": 0
}
```

安全上应限制为：

- 只监听本机。
- 使用 Unix Socket 权限或随机访问令牌。
- 证券代码必须通过格式校验。
- SQL 必须由程序按照固定模板生成。
- 禁止外部提交任意 SQL。
- 只开放读取接口。

## 八、不可行或不推荐的路径

### 8.1 独立进程直接加载 TBAPI2

外部 Python、Java 或 C++ 程序直接 `dlopen` TBAPI2，无法自动获得万得进程中的全局单例和登录状态。

因此，单纯在外部进程调用：

```text
CJAVAQuery(...)
```

通常不能继承已经登录的会话。

### 8.2 使用 CJAVAConnectAsTS 附着终端

当前 macOS 版本中的 `CJAVAConnectAsTS` 只是一个返回错误码的空壳，不能用于附着到已经运行的万得终端。

### 8.3 读取或复制登录凭据

不建议尝试提取密码、令牌或完整进程内存。当前进程内调用方案本身不需要这些信息。

### 8.4 OCR 或界面自动化

通过截图 OCR 读取“3、2、300 万、200 万”可以作为临时替代，但稳定性较差：

- 窗口必须保持显示。
- 分辨率和缩放会影响识别。
- 页面布局升级会导致失效。
- 难以支持大量 ETF。

## 九、风险和限制

### 9.1 私有接口稳定性

TBAPI2 是客户端私有接口，万得升级后以下内容都可能变化：

- 导出函数名称。
- 函数参数。
- 对象内存布局。
- 回调签名。
- JavaTableFrame 格式。
- 错误码含义。

实现时应绑定具体客户端版本，并在升级后重新验证。

### 9.2 进程稳定性

错误的函数签名、对象大小或数据帧解析可能导致万得客户端崩溃。正式实现前应：

- 先在非交易环境测试。
- 确保探针不拦截或修改原有订阅。
- 对所有长度和指针做边界检查。
- 在异常时立即停止订阅。

### 9.3 数据授权

即使技术上能够读取，也应向万得确认当前账号的数据授权是否允许：

- 自动化访问。
- 持续保存。
- 提供给其他程序。
- 二次分发或商业使用。

优先选择万得官方 API 或获得书面授权。

## 十、当前环境状态

- 探针已经成功加载到当前万得进程。
- 探针没有后台线程，不会持续提交查询。
- 退出万得客户端后，已加载的探针会自动卸载。
- 临时 dylib 和结果文件仍位于万得的 `Data/tmp` 目录。
- 工作区保留了探针源代码和说明，便于以后继续。

## 十一、最终判断

| 项目 | 判断 |
|---|---|
| 能否定位数据源 | 已完成 |
| 能否确认 SQL 和字段 | 已完成 |
| 能否附加已登录万得进程 | 已验证可行 |
| 能否在进程内加载只读探针 | 已验证可行 |
| 能否复用进程内 TBAPI2 单例 | 调用链和实际加载均已验证 |
| 一次性 `CJAVAQuery` 是否适用 | 不适用，返回 `-12314` |
| 正确实现方向 | `CreateSubscription → ModifySubscription → 回调` |
| 是否已经解析出实际返回值 | 尚未完成 |
| 技术上是否值得继续 | 值得，关键障碍已经缩小到订阅回调和数据帧解析 |

最终结论：

> 已登录万得后，通过外部程序临时进入万得进程并复用其 TBAPI2 会话是可行的。实时申购赎回信息需要通过订阅接口获取。剩余工作主要是注册订阅回调、解析 JavaTableFrame，并将结果通过受限的本地接口提供给外部程序。
